% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{forecast_tvvar}
\alias{forecast_tvvar}
\title{Multi-step forecasts for a tvvar_fit (unpenalized ML)}
\usage{
forecast_tvvar(
  fit,
  h = 8,
  B = 500,
  seed = NULL,
  intervals = c(0.16, 0.5, 0.84),
  use_param_draws = TRUE
)
}
\arguments{
\item{fit}{tvvar_fit from unpenalized_estimate(..., method = "ML")}

\item{h}{integer horizon (number of steps ahead)}

\item{B}{number of Monte Carlo paths (set B=1 for plug-in path)}

\item{seed}{optional integer seed (reproducibility)}

\item{intervals}{numeric probs to report (default 16\%, 50\%, 84\%)}

\item{use_param_draws}{logical; if FALSE, uses plug-in theta only}
}
\value{
list with elements:
\item{mean, median, lb, ub}{N x h matrices of forecast stats}
\item{paths}{optional B x h x N array (only if B not too large)}
}
\description{
Simulates recursive forecasts y_{T+1}, …, y_{T+h} using parameter draws
from the asymptotic normal N(theta, vcov) and simulated factor paths.
Volatility is propagated with a simple BEKK(1,1) recursion using simulated shocks.
}
